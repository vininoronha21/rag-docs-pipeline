{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "if [ -z \"$NEXT_PUBLIC_BACKEND_URL\" ]; then printf 'NEXT_PUBLIC_BACKEND_URL must be set in the Vercel project environment.\\n' >&2; exit 1; fi; npm run build"
}
